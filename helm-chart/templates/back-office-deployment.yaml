apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backoffice.name }}-deployment
  namespace: {{ .Values.backoffice.namespace}}
spec:
  replicas: {{ .Values.backoffice.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.backoffice.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.backoffice.name }}
    spec:
      containers:
        - name: {{ .Values.backoffice.name }}
          image: {{ .Values.image.repository }}/{{ .Values.image.group }}/{{ .Values.backoffice.imagename }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy}}
          ports:
            - containerPort: {{ .Values.backoffice.targetPort }}
          env:
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: password
            - name: MYSQL_URL
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: url
            - name: SMALLRYE_JWT_SIGN_KEY_LOCATION
              value: {{ .Values.authkeys.privateloc}}
            - name: MP_JWT_VERIFY_PUBLICKEY_LOCATION
              value: {{ .Values.authkeys.publicloc}}
          volumeMounts:
            - name: jwt-keys
              mountPath: {{ .Values.authkeys.mountloc}}
      volumes:
        - name: jwt-keys
          secret:
            secretName: jwt-keys
      imagePullSecrets:
        - name: my-registry-secret

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backoffice.name }}-deployment
  namespace: back-office
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.backoffice.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.backoffice.name }}
    spec:
      containers:
        - name: {{ .Values.backoffice.name }}
          image: {{ .Values.image.repository }}/{{ .Values.image.group }}/back-office:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy}}
          ports:
            - containerPort: {{ .Values.backoffice.targetPort }}
          env:
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: password
            - name: MYSQL_URL
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: url
            - name: SMALLRYE_JWT_SIGN_KEY_LOCATION
              value: {{ .Values.auth-keys.private-location}}
            - name: MP_JWT_VERIFY_PUBLICKEY_LOCATION
              value: {{ .Values.auth-keys.public-location}}
          volumeMounts:
            - name: jwt-keys
              mountPath: /src/main/resources
      volumes:
        - name: jwt-keys
          secret:
            secretName: jwt-keys
      imagePullSecrets:
        - name: my-registry-secret

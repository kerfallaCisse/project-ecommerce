apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-management-deployment
  namespace: stock-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stock-management
  template:
    metadata:
      labels:
        app: stock-management
    spec:
      containers:
        - name: stock-management
          image: registry.gitlab.unige.ch/pinfo-2023/pinfo-2/project-ecommerce/stock-management:0.1.0-SNAPSHOT
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: username
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: password
            - name: MYSQL_URL
              valueFrom:
                secretKeyRef:
                  name: my-database-secret
                  key: url
      imagePullSecrets:
        - name: my-registry-secret
